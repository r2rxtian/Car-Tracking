# Optional when the frontend and backend use different public origins.
# VITE_TRACKING_SERVER_URL=https://tracking.example.com
PORT=3001
CLIENT_ORIGIN=http://localhost:5173
# Optional demo movement. Keep false when using real phone GPS.
ENABLE_SIMULATOR=false
